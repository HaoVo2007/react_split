const HomePage = () => (
  <main className="flex min-h-screen flex-col items-center justify-center bg-slate-50 text-slate-700">
    <p className="text-lg font-medium">Home page content will be available soon.</p>
    <p className="mt-2 text-sm text-slate-500">Navigate to /auth/login or /auth/register to preview the new auth experience.</p>
  </main>
)

export default HomePage
